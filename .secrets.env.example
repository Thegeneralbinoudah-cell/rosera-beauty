# انسخي إلى .secrets.env (هذا الملف مُهمل من git) ثم ضعي المفتاح الحقيقي في سطر واحد:
#   cp .secrets.env.example .secrets.env
OPENAI_API_KEY=

# لمزامنة منشآت Google إلى Supabase (محلي فقط — لا ترفعي service role إلى git):
# SUPABASE_SERVICE_ROLE_KEY=...
# ثم: npm run seed:eastern-places
